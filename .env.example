# ── 키 발급 후 이 파일을 복사해 `.env` 로 만들고 값을 채우세요 ──
# (.env 는 .gitignore 에 의해 커밋되지 않습니다)

# 카카오 지도 JavaScript 키 (지도 표시용)
#  Kakao Developers > 내 애플리케이션 > 앱 키 > "JavaScript 키"
#  플랫폼 > Web 에 http://localhost:5173 등록 필요
VITE_KAKAO_JS_KEY=

# 카카오 REST API 키 (로컬 키워드 검색 = 빵집 좌표 보강용)
#  같은 앱의 "REST API 키"
#  CP6-4: 대전한바퀴 자동차 이동시간(카카오모빌리티 길찾기)도 이 키를 그대로 재사용한다.
#  무료 쿼터(일 10,000건) 안에서는 별도 제휴 신청 없이 바로 호출 가능(확인됨).
VITE_KAKAO_REST_KEY=

# ODsay 대중교통 API 키 — CP12: 런타임 호출은 걷어냄(요금표는 일 1,000건이지만 실측 결과
#  현재 이 키는 30여 건만에 429 쿼터 초과가 뜬다, 하루 7코스면 소진). 지금은 안 부르지만
#  쿼터가 복구되면 되살릴 수 있게 odsay.js/키는 남겨둔다.
VITE_ODSAY_API_KEY=

# 한국관광공사 OpenAPI 서비스키 (KorService2)
#  data.go.kr > "한국관광공사 국문 관광정보 서비스" 활용신청 > 일반 인증키(Decoding) 값
VITE_TOUR_API_KEY=

# Supabase 프로젝트 (구글/카카오 계정 연동 + 찜한 빵집 DB 저장용)
#  supabase.com > 프로젝트 생성 > Project Settings > API
#  Google/Kakao 로그인은 Supabase 대시보드 Authentication > Providers 에서 별도 설정 필요
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# TMAP 보행자 경로안내 API 키 (대전한바퀴 도보 실측, CP12)
#  openapi.sk.com 가입 > 앱 생성 > "경로안내" 상품 Free 신청 > appKey
#  Free 한도 1,000건/일 (자동차·보행자·타임머신 합산 그룹 쿼터 — 자동차는 카카오를 쓰므로
#  이 한도를 전부 도보에 쓸 수 있다). 상세: docs/가이드_TMAP_보행자API.md
VITE_TMAP_APP_KEY=
