# 마이페이지 신설 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** NavBar의 "나만의 리스트"를 "마이페이지"로 교체하고, 프로필 카드 + 찜한 빵/찜한 코스/기록장(빵집 연결)/친구목록(placeholder) 4개 패널을 제공한다.

**Architecture:** 새 라우팅 라이브러리 없이 기존 `LandingPage.jsx`의 boolean-state 페이지 전환 패턴을 그대로 확장한다. `MyPage.jsx`가 내부적으로 `panel` state(`home`/`bakeries`/`courses`/`diary`/`friends`)로 리스트↔상세를 전환한다. 각 패널은 독립된 컴포넌트 + 독립된 데이터 훅으로 분리한다.

**Tech Stack:** React 18 (JSX, hooks), Supabase JS client(`@supabase/supabase-js`), Kakao Maps JS SDK(`useKakaoLoader`), 순수 CSS(`src/styles.css`, CSS custom properties), Node 내장 테스트러너(`node --test`, `node:assert/strict`) — 이 프로젝트엔 React 컴포넌트 테스트 도구(jsdom/RTL)가 없으므로 순수 함수만 자동 테스트하고 컴포넌트/훅은 `npm run dev`로 직접 확인한다.

**Spec:** `docs/superpowers/specs/2026-08-18-mypage-design.md`

## Global Constraints

- 사진 첨부 없음 — 이번 범위에서 완전히 제외 (Supabase Storage 버킷 도입 안 함).
- 친구 시스템 실제 로직 없음 — 탭/패널 자리와 "준비 중" 빈 상태만.
- 새 라우팅 라이브러리(react-router 등) 도입 금지 — 기존 boolean-state 전환 패턴 유지.
- 신규 Supabase 테이블은 `saved_bakeries`/`saved_courses`와 동일한 RLS 패턴(select/insert own, 필요 시 update/delete own) 사용.
- CSS는 `src/styles.css`의 루트 커스텀 프로퍼티(`var(--brown)`, `var(--accent)`, `var(--line)`, `var(--card)`, `var(--muted)`, `var(--ink)`, `var(--gold)`)를 그대로 사용하고, 랜딩 전용 `--bm-*` 토큰은 쓰지 않는다(마이페이지는 앱 페이지이지 랜딩 섹션이 아님).
- 버튼은 기존 `.primary-btn`/`.ghost-btn` 클래스를 재사용한다(새 버튼 스타일 만들지 않음).
- 표시 이름은 `nickname > full_name > name > email` 우선순위로 통일(`getDisplayName` 헬퍼 하나로 `AuthMenu`/`MyPage` 양쪽에서 공유).
- Supabase 스키마 변경(`diary_entries`)은 CLI 마이그레이션이 연결되어 있지 않으므로 **사람이 Supabase 대시보드 SQL Editor에서 수동 실행**해야 한다 — 이 프로젝트의 기존 `supabase/schema.sql` 파일 상단 주석에 이미 명시된 방식이다. 에이전트는 SQL을 파일에 추가하는 것까지만 하고, 실행은 사람에게 안내한다.

---

## Task 1: 순수 헬퍼 — 표시 이름 / 코스 라벨

**Files:**
- Create: `src/lib/displayName.js`
- Create: `src/lib/displayName.test.js`
- Create: `src/lib/courseLabel.js`
- Create: `src/lib/courseLabel.test.js`

**Interfaces:**
- Produces: `getDisplayName(user: { user_metadata?: { nickname?, full_name?, name? }, email?: string } | null): string | null`
- Produces: `formatCourseLabel(row: { title: string, travel_mode: 'car'|'transit'|'walk', stops: Array, created_at: string }): string`

- [ ] **Step 1: 실패하는 테스트 작성 — `getDisplayName`**

```js
// src/lib/displayName.test.js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { getDisplayName } from './displayName.js'

test('getDisplayName: nickname 이 있으면 최우선', () => {
  const user = { user_metadata: { nickname: '장발장', full_name: 'Jean', name: 'JV' }, email: 'a@b.com' }
  assert.equal(getDisplayName(user), '장발장')
})

test('getDisplayName: nickname 없으면 full_name', () => {
  const user = { user_metadata: { full_name: 'Jean' }, email: 'a@b.com' }
  assert.equal(getDisplayName(user), 'Jean')
})

test('getDisplayName: full_name 없으면 name', () => {
  const user = { user_metadata: { name: 'JV' }, email: 'a@b.com' }
  assert.equal(getDisplayName(user), 'JV')
})

test('getDisplayName: metadata 없으면 email', () => {
  const user = { user_metadata: {}, email: 'a@b.com' }
  assert.equal(getDisplayName(user), 'a@b.com')
})

test('getDisplayName: 아무것도 없으면 기본값', () => {
  const user = { user_metadata: {} }
  assert.equal(getDisplayName(user), '내 계정')
})

test('getDisplayName: user 가 null 이면 null', () => {
  assert.equal(getDisplayName(null), null)
})
```

- [ ] **Step 2: 테스트 실행해서 실패 확인**

Run: `node --test src/lib/displayName.test.js`
Expected: FAIL (`displayName.js` 파일이 없어서 모듈을 찾지 못함)

- [ ] **Step 3: 최소 구현 작성**

```js
// src/lib/displayName.js
// 표시용 이름 우선순위: 닉네임(직접 설정) > OAuth full_name > OAuth name > email > 기본값.
// AuthMenu(상단 메뉴)와 MyPage(프로필 카드) 양쪽에서 동일하게 사용한다.
export function getDisplayName(user) {
  if (!user) return null
  return (
    user.user_metadata?.nickname ||
    user.user_metadata?.full_name ||
    user.user_metadata?.name ||
    user.email ||
    '내 계정'
  )
}
```

- [ ] **Step 4: 테스트 실행해서 통과 확인**

Run: `node --test src/lib/displayName.test.js`
Expected: PASS (6 tests)

- [ ] **Step 5: 실패하는 테스트 작성 — `formatCourseLabel`**

```js
// src/lib/courseLabel.test.js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { formatCourseLabel } from './courseLabel.js'

test('formatCourseLabel: 제목/날짜/스탑 수/이동수단을 조합한다', () => {
  const row = {
    title: '대전한바퀴',
    travel_mode: 'car',
    stops: [{ id: 'a' }, { id: 'b' }, { id: 'c' }],
    created_at: '2026-08-18T12:00:00.000Z',
  }
  assert.equal(formatCourseLabel(row), '대전한바퀴 · 8/18 저장 · 3곳 · 자동차')
})

test('formatCourseLabel: 이동수단 매핑 — transit/walk', () => {
  const base = { title: '대전한바퀴', stops: [], created_at: '2026-01-05T00:00:00.000Z' }
  assert.equal(formatCourseLabel({ ...base, travel_mode: 'transit' }), '대전한바퀴 · 1/5 저장 · 0곳 · 대중교통')
  assert.equal(formatCourseLabel({ ...base, travel_mode: 'walk' }), '대전한바퀴 · 1/5 저장 · 0곳 · 도보')
})

test('formatCourseLabel: 알 수 없는 이동수단은 원본 값을 그대로 노출', () => {
  const row = { title: '대전한바퀴', travel_mode: 'bike', stops: [], created_at: '2026-01-05T00:00:00.000Z' }
  assert.equal(formatCourseLabel(row), '대전한바퀴 · 1/5 저장 · 0곳 · bike')
})
```

- [ ] **Step 6: 테스트 실행해서 실패 확인**

Run: `node --test src/lib/courseLabel.test.js`
Expected: FAIL (`courseLabel.js` 파일이 없어서 모듈을 찾지 못함)

- [ ] **Step 7: 최소 구현 작성**

```js
// src/lib/courseLabel.js
const MODE_LABEL = { car: '자동차', transit: '대중교통', walk: '도보' }

// saved_courses row → 마이페이지 목록/상세 헤더에 쓰는 한 줄 라벨.
// saved_courses 는 title 이 항상 "대전한바퀴" 로 고정 저장되므로(코스별 커스텀 이름 없음),
// 날짜/스탑 수/이동수단으로 각 저장 건을 구분한다.
export function formatCourseLabel(row) {
  const date = new Date(row.created_at)
  const dateLabel = `${date.getMonth() + 1}/${date.getDate()}`
  const stopCount = row.stops?.length ?? 0
  const modeLabel = MODE_LABEL[row.travel_mode] || row.travel_mode
  return `${row.title} · ${dateLabel} 저장 · ${stopCount}곳 · ${modeLabel}`
}
```

- [ ] **Step 8: 테스트 실행해서 통과 확인**

Run: `node --test src/lib/courseLabel.test.js`
Expected: PASS (3 tests)

- [ ] **Step 9: 전체 테스트 스위트 실행 (회귀 확인)**

Run: `npm test`
Expected: PASS (기존 테스트 포함 전부 통과)

- [ ] **Step 10: Commit**

```bash
git add src/lib/displayName.js src/lib/displayName.test.js src/lib/courseLabel.js src/lib/courseLabel.test.js
git commit -m "[FE] [FEAT] 표시 이름/코스 라벨 순수 헬퍼 추가"
```

---

## Task 2: 닉네임 설정 + 표시 이름 통일 (`useAuth`, `AuthMenu`)

**Files:**
- Modify: `src/hooks/useAuth.js`
- Modify: `src/components/auth/AuthMenu.jsx:1-4,90-91`

**Interfaces:**
- Consumes: `getDisplayName(user)` from Task 1 (`src/lib/displayName.js`)
- Produces: `useAuth()` now also returns `updateNickname(nickname: string): Promise<{ error }>`— 이후 Task 4 의 `ProfileCard` 가 이걸 사용한다.

- [ ] **Step 1: `useAuth.js` 에 `updateNickname` 추가**

`src/hooks/useAuth.js` 의 `signOut` 정의 바로 아래(현재 47번째 줄)에 추가하고, 반환 객체에도 포함시킨다:

```js
  const signOut = () => supabase?.auth.signOut()

  // 프로필 편집(닉네임 변경). user_metadata.nickname 에 저장 — getDisplayName 이 최우선으로 읽는다.
  const updateNickname = (nickname) => supabase.auth.updateUser({ data: { nickname } })

  return { user, loading, signInWithGoogle, signInWithKakao, signOut, updateNickname }
```

(기존 `return { user, loading, signInWithGoogle, signInWithKakao, signOut }` 줄을 위 코드로 교체)

- [ ] **Step 2: `AuthMenu.jsx` 에서 `getDisplayName` 사용**

`src/components/auth/AuthMenu.jsx` 상단 import 블록(4번째 줄 `import { supabaseEnabled } from '../../lib/supabase'` 다음)에 추가:

```js
import { getDisplayName } from '../../lib/displayName'
```

`AuthMenu` 함수 내부의 아래 두 줄(현재 90-91번째 줄)을:

```js
    const name =
      user.user_metadata?.full_name || user.user_metadata?.name || user.email || '내 계정'
```

이렇게 교체:

```js
    const name = getDisplayName(user)
```

- [ ] **Step 3: 수동 확인 — 개발 서버**

Run: `npm run dev`

브라우저에서:
1. 우측 상단 "로그인" → Google 또는 Kakao로 로그인
2. 상단 메뉴에 기존과 동일하게 이름(또는 이메일)이 표시되는지 확인 (닉네임을 아직 설정 안 했으므로 `full_name`/`name`/`email` 순으로 그대로 보여야 함 — 회귀 없음 확인)
3. 로그아웃 시 "로그인" 버튼으로 돌아가는지 확인

- [ ] **Step 4: Commit**

```bash
git add src/hooks/useAuth.js src/components/auth/AuthMenu.jsx
git commit -m "[FE] [FEAT] 닉네임 변경 지원 및 표시 이름 로직 통일"
```

---

## Task 3: DB 스키마 — `diary_entries` 테이블

**Files:**
- Modify: `supabase/schema.sql` (파일 끝에 추가)

**Interfaces:**
- Produces: Supabase 테이블 `diary_entries(id, user_id, bakery_id, bakery jsonb, text, created_at, updated_at)` — Task 7 의 `useDiaryEntries` 훅이 이 테이블을 select/insert/update/delete 한다.

- [ ] **Step 1: `schema.sql` 끝에 테이블 정의 추가**

`supabase/schema.sql` 파일 맨 끝(현재 47번째 줄, `saved_courses_delete_own` 정책 다음)에 추가:

```sql

-- 마이페이지 "기록장": 빵집 상세에서 남기는 짧은 기록. 사진 첨부는 이번 범위에서 제외.
create table if not exists diary_entries (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  bakery_id text not null,
  bakery jsonb not null,
  text text not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

alter table diary_entries enable row level security;

create policy "diary_entries_select_own" on diary_entries
  for select using (auth.uid() = user_id);

create policy "diary_entries_insert_own" on diary_entries
  for insert with check (auth.uid() = user_id);

create policy "diary_entries_update_own" on diary_entries
  for update using (auth.uid() = user_id);

create policy "diary_entries_delete_own" on diary_entries
  for delete using (auth.uid() = user_id);
```

- [ ] **Step 2: 사람이 Supabase 대시보드에서 수동 실행 (에이전트가 대신 할 수 없음)**

이 프로젝트는 Supabase CLI/마이그레이션이 연결되어 있지 않다(`schema.sql` 파일 상단 주석 참고). 사용자에게 알린다:

> "Supabase 대시보드 → SQL Editor에서 `supabase/schema.sql`에 새로 추가된 `diary_entries` 블록(Step 1 내용)을 실행해주세요. 실행 후 Table Editor에 `diary_entries` 테이블이 보이면 완료입니다."

이 단계가 완료되기 전까지 Task 7(기록장 기능)은 코드는 동작하지만 실제 DB 호출은 에러가 난다 — Task 8 전체 회귀 테스트 전까지 완료되면 된다.

- [ ] **Step 3: Commit**

```bash
git add supabase/schema.sql
git commit -m "[FE] [FEAT] 기록장(diary_entries) 테이블 스키마 추가"
```

---

## Task 4: 마이페이지 뼈대 — 프로필 카드 + 패널 라우팅 + 내비게이션 연결

**Files:**
- Create: `src/pages/MyPage.jsx`
- Create: `src/components/mypage/ProfileCard.jsx`
- Modify: `src/components/landing/NavBar.jsx`
- Modify: `src/pages/LandingPage.jsx`
- Modify: `src/styles.css` (끝에 추가)

**Interfaces:**
- Consumes: `useAuth()` (Task 2 의 `updateNickname` 포함), `getDisplayName` (Task 1)
- Produces: `<MyPage />` — panel state `'home' | 'bakeries' | 'courses' | 'diary' | 'friends'`. Task 5/6/7 이 각각 `'bakeries'`/`'courses'`/`'diary'` 분기에 실제 패널 컴포넌트를 끼워 넣는다(지금은 그 세 분기가 없고 `'home'`/`'friends'`만 존재).
- Produces: CSS 클래스 `.mypage-gate`, `.mypage-home`, `.mypage-profile`, `.mypage-avatar`, `.mypage-name`, `.mypage-nickname-form`, `.mypage-nickname-input`, `.mypage-nickname-actions`, `.mypage-panel-grid`, `.mypage-panel-card`, `.mypage-panel-card-disabled`, `.mypage-panel-icon`, `.mypage-panel`, `.mypage-panel-header`, `.mypage-back` — 이후 Task 5/6/7 이 재사용한다.

- [ ] **Step 1: `ProfileCard.jsx` 작성**

```jsx
// src/components/mypage/ProfileCard.jsx
import { useState } from 'react'
import { useAuth } from '../../hooks/useAuth'
import { getDisplayName } from '../../lib/displayName'

// 마이페이지 왼쪽 프로필 카드. 아바타는 이니셜 placeholder(이미지 업로드 없음),
// 닉네임만 편집 가능 — user_metadata.nickname 에 저장되고 getDisplayName 이 최우선으로 읽는다.
export default function ProfileCard() {
  const { user, updateNickname } = useAuth()
  const [editing, setEditing] = useState(false)
  const [draft, setDraft] = useState('')
  const [saving, setSaving] = useState(false)

  const name = getDisplayName(user)
  const initial = name?.[0] || '?'

  const startEdit = () => {
    setDraft(name === '내 계정' ? '' : name)
    setEditing(true)
  }

  const submit = async (e) => {
    e.preventDefault()
    const trimmed = draft.trim()
    if (!trimmed) return
    setSaving(true)
    const { error } = await updateNickname(trimmed)
    setSaving(false)
    if (error) {
      console.error('[프로필] 닉네임 변경 실패', error)
      return
    }
    setEditing(false)
  }

  return (
    <div className="mypage-profile">
      <div className="mypage-avatar" aria-hidden="true">{initial}</div>
      {editing ? (
        <form className="mypage-nickname-form" onSubmit={submit}>
          <input
            className="mypage-nickname-input"
            value={draft}
            onChange={(e) => setDraft(e.target.value)}
            maxLength={20}
            autoFocus
          />
          <div className="mypage-nickname-actions">
            <button type="submit" className="primary-btn" disabled={saving}>
              {saving ? '저장 중…' : '저장'}
            </button>
            <button type="button" className="ghost-btn" onClick={() => setEditing(false)}>
              취소
            </button>
          </div>
        </form>
      ) : (
        <>
          <h3 className="mypage-name">{name}</h3>
          <button type="button" className="ghost-btn" onClick={startEdit}>
            프로필 편집
          </button>
        </>
      )}
    </div>
  )
}
```

- [ ] **Step 2: `MyPage.jsx` 작성**

```jsx
// src/pages/MyPage.jsx
import { useState } from 'react'
import { useAuth } from '../hooks/useAuth'
import ProfileCard from '../components/mypage/ProfileCard'

// 마이페이지 — 프로필 카드 + 찜한 빵/찜한 코스/기록장/친구목록(준비중) 4개 패널.
// 로그인 필수: 비로그인 상태면 안내만 보여준다(로그인은 상단 AuthMenu 에서).
// panel 이 'home' 이 아니면 해당 패널만 전체 화면으로 보여주고 '‹' 로 home 으로 돌아간다.
export default function MyPage() {
  const { user, loading } = useAuth()
  const [panel, setPanel] = useState('home') // 'home' | 'bakeries' | 'courses' | 'diary' | 'friends'

  if (loading) return null

  if (!user) {
    return (
      <div className="mypage-gate">
        <h2>마이페이지</h2>
        <p>로그인하면 찜한 빵/코스와 기록장을 모아볼 수 있어요.</p>
      </div>
    )
  }

  if (panel === 'friends') {
    return (
      <div className="mypage-panel">
        <div className="mypage-panel-header">
          <button type="button" className="mypage-back" onClick={() => setPanel('home')}>
            ‹
          </button>
          <h3>친구목록</h3>
        </div>
        <p className="saved-empty">친구 기능은 준비 중이에요.</p>
      </div>
    )
  }

  if (panel !== 'home') {
    // Task 5/6/7 에서 'bakeries'/'courses'/'diary' 분기가 여기 추가된다.
    return null
  }

  return (
    <div className="mypage-home">
      <ProfileCard />
      <div className="mypage-panel-grid">
        <button type="button" className="mypage-panel-card" onClick={() => setPanel('bakeries')}>
          <span className="mypage-panel-icon" aria-hidden="true">❤️</span>
          찜한 빵 목록
        </button>
        <button type="button" className="mypage-panel-card" onClick={() => setPanel('courses')}>
          <span className="mypage-panel-icon" aria-hidden="true">📍</span>
          찜한 코스 목록
        </button>
        <button type="button" className="mypage-panel-card" onClick={() => setPanel('diary')}>
          <span className="mypage-panel-icon" aria-hidden="true">📄</span>
          기록장
        </button>
        <button
          type="button"
          className="mypage-panel-card mypage-panel-card-disabled"
          onClick={() => setPanel('friends')}
        >
          <span className="mypage-panel-icon" aria-hidden="true">👥</span>
          친구목록
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: `NavBar.jsx` — "나만의 리스트" → "마이페이지"**

`src/components/landing/NavBar.jsx` 에서 `onOpenSaved` prop 이름을 `onOpenMyPage` 로, 버튼 라벨 "나만의 리스트"를 "마이페이지"로 바꾼다. 데스크톱(현재 14번째 줄 prop 선언, 48-50번째 줄 버튼)과 모바일 메뉴(113-121번째 줄 버튼) 둘 다 수정.

`export default function NavBar({` 블록의 prop 목록에서:
```js
  onOpenSaved,
```
→
```js
  onOpenMyPage,
```

데스크톱 네비게이션의:
```jsx
          <button type="button" onClick={onOpenSaved}>
            나만의 리스트
          </button>
```
→
```jsx
          <button type="button" onClick={onOpenMyPage}>
            마이페이지
          </button>
```

모바일 메뉴의:
```jsx
            <button
              type="button"
              onClick={() => {
                onOpenSaved()
                setNavOpen(false)
              }}
            >
              나만의 리스트
            </button>
```
→
```jsx
            <button
              type="button"
              onClick={() => {
                onOpenMyPage()
                setNavOpen(false)
              }}
            >
              마이페이지
            </button>
```

- [ ] **Step 4: `LandingPage.jsx` — `MyPage` 로 교체**

`src/pages/LandingPage.jsx` 에서:

`import SavedListPage from './SavedListPage'` (7번째 줄) →
```js
import MyPage from './MyPage'
```

`const [showSaved, setShowSaved] = useState(false)` (26번째 줄) →
```js
const [showMyPage, setShowMyPage] = useState(false)
```

`openSaved` 함수(52-59번째 줄) 전체를:
```js
  const openSaved = () => {
    setShowSaved(true)
    setFeatureOpen(false)
    setShowInfo(false)
    setShowMap(false)
    setShowTour(false)
    setShowPilgrimage(false)
  }
```
→
```js
  const openMyPage = () => {
    setShowMyPage(true)
    setFeatureOpen(false)
    setShowInfo(false)
    setShowMap(false)
    setShowTour(false)
    setShowPilgrimage(false)
  }
```

이 파일 안의 다른 `open*` 함수들(`openInfo`, `openBakeryMap`, `openTour`, `openPilgrimage`) 안에 있는 `setShowSaved(false)` 호출들을 모두 `setShowMyPage(false)` 로 바꾼다 (총 4곳: 63번째, 75번째, 85번째, 97번째 줄 부근).

`goHome` 함수(102-109번째 줄) 안의 `setShowSaved(false)` 도 `setShowMyPage(false)` 로 바꾼다.

`<NavBar ... onOpenSaved={openSaved} />` (124번째 줄) →
```jsx
        onOpenMyPage={openMyPage}
```

```jsx
      {showSaved && (
        <div className="page">
          <SavedListPage />
        </div>
      )}
```
(169-173번째 줄) →
```jsx
      {showMyPage && (
        <div className="page">
          <MyPage />
        </div>
      )}
```

마지막으로 홈 화면 노출 조건(185번째 줄)의 `!showSaved` 를 `!showMyPage` 로 바꾼다:
```jsx
      {!showInfo && !showMap && !showTour && !showPilgrimage && !showMyPage && !featureOpen && (
```

- [ ] **Step 5: CSS 추가**

`src/styles.css` 파일 맨 끝에 추가:

```css

/* ===== 마이페이지 ===== */
.mypage-gate{ max-width:480px; margin:80px auto; text-align:center; color:var(--muted); }
.mypage-gate h2{ color:var(--brown); margin-bottom:10px; }
.mypage-home{ display:grid; grid-template-columns:240px 1fr; gap:24px; align-items:start; }
.mypage-profile{ display:flex; flex-direction:column; align-items:center; gap:10px; border:1px solid var(--line); border-radius:16px; padding:24px 16px; background:var(--card); text-align:center; }
.mypage-avatar{ width:80px; height:80px; border-radius:50%; background:var(--gold); color:#fff; font-size:1.8rem; font-weight:800; display:flex; align-items:center; justify-content:center; }
.mypage-name{ margin:0; color:var(--brown); }
.mypage-nickname-form{ display:flex; flex-direction:column; gap:8px; width:100%; }
.mypage-nickname-input{ padding:8px 12px; border:1.5px solid var(--line); border-radius:999px; font-size:.9rem; text-align:center; }
.mypage-nickname-actions{ display:flex; gap:8px; justify-content:center; }
.mypage-panel-grid{ display:grid; grid-template-columns:repeat(2,1fr); gap:16px; }
.mypage-panel-card{ display:flex; flex-direction:column; align-items:center; justify-content:center; gap:8px; padding:32px 16px; border:1px solid var(--line); border-radius:16px; background:var(--card); font-weight:700; color:var(--brown); }
.mypage-panel-card:hover{ border-color:var(--accent); }
.mypage-panel-card-disabled{ opacity:.55; }
.mypage-panel-icon{ font-size:1.6rem; }
.mypage-panel{ max-width:720px; margin:0 auto; }
.mypage-panel-header{ display:flex; align-items:center; gap:10px; margin-bottom:16px; }
.mypage-panel-header h3{ margin:0; color:var(--brown); }
.mypage-back{ border:none; background:none; font-size:1.4rem; color:var(--brown); cursor:pointer; line-height:1; padding:4px 8px; }
@media (max-width:760px){
  .mypage-home{ grid-template-columns:1fr; }
  .mypage-panel-grid{ grid-template-columns:1fr; }
}
```

- [ ] **Step 6: 수동 확인 — 개발 서버**

Run: `npm run dev`

브라우저에서:
1. 상단 메뉴에 "마이페이지" 버튼이 보이는지 확인 (기존 "나만의 리스트" 자리)
2. 비로그인 상태로 "마이페이지" 클릭 → "로그인하면..." 안내 문구가 보이는지 확인
3. 로그인 → "마이페이지" 클릭 → 프로필 카드(아바타 이니셜 + 이름) + 4개 패널 카드(찜한 빵/찜한 코스/기록장/친구목록)가 보이는지 확인
4. "프로필 편집" 클릭 → 닉네임 입력 → 저장 → 카드에 새 닉네임이 반영되는지, 새로고침 후에도 유지되는지 확인
5. "친구목록" 카드 클릭 → "친구 기능은 준비 중이에요" 표시 확인, ‹ 로 뒤로가기 확인
6. "찜한 빵 목록"/"찜한 코스 목록"/"기록장" 카드 클릭 시 지금은 빈 화면(null)이 뜨는 게 정상 — Task 5/6/7 에서 채워진다
7. 모바일 너비(760px 이하)로 줄여서 프로필 카드가 위, 패널 그리드가 1열로 쌓이는지 확인

- [ ] **Step 7: Commit**

```bash
git add src/pages/MyPage.jsx src/components/mypage/ProfileCard.jsx src/components/landing/NavBar.jsx src/pages/LandingPage.jsx src/styles.css
git commit -m "[FE] [FEAT] 마이페이지 뼈대 — 프로필 카드, 패널 라우팅, 내비게이션 연결"
```

---

## Task 5: 찜한 빵 목록 패널

**Files:**
- Create: `src/components/mypage/SavedBakeriesPanel.jsx`
- Modify: `src/pages/MyPage.jsx`
- Modify: `src/styles.css` (끝에 추가)

**Interfaces:**
- Consumes: `useSavedBakeries()` (기존, `src/hooks/useSavedBakeries.js` — 변경 없음)
- Produces: `<SavedBakeriesPanel onBack={() => void} />`

- [ ] **Step 1: `SavedBakeriesPanel.jsx` 작성**

```jsx
// src/components/mypage/SavedBakeriesPanel.jsx
import { useSavedBakeries } from '../../hooks/useSavedBakeries'

// 마이페이지 찜한 빵 목록 패널. useSavedBakeries 는 그대로 재사용하고
// 카드 그리드 UI만 시안(대전 웹사이트 4.pdf page 11/12)에 맞게 새로 구성한다.
export default function SavedBakeriesPanel({ onBack }) {
  const { saved, toggleSave } = useSavedBakeries()

  return (
    <div className="mypage-panel">
      <div className="mypage-panel-header">
        <button type="button" className="mypage-back" onClick={onBack}>
          ‹
        </button>
        <h3>찜한 빵 목록{saved.length > 0 && ` (${saved.length}곳)`}</h3>
      </div>
      {saved.length === 0 ? (
        <p className="saved-empty">아직 찜한 빵집이 없어요.</p>
      ) : (
        <div className="mypage-card-grid">
          {saved.map((b) => (
            <div key={b.id} className="mypage-item-card">
              <span className="mypage-item-name">{b.name}</span>
              {b.address && <span className="mypage-item-sub">{b.address}</span>}
              <button type="button" className="save-btn saved" onClick={() => toggleSave(b)}>
                ❤️ 찜 해제
              </button>
            </div>
          ))}
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2: `MyPage.jsx` 에 연결**

`src/pages/MyPage.jsx` 상단 import 에 추가:

```js
import SavedBakeriesPanel from '../components/mypage/SavedBakeriesPanel'
```

Task 4 에서 만든 아래 블록:

```jsx
  if (panel !== 'home') {
    // Task 5/6/7 에서 'bakeries'/'courses'/'diary' 분기가 여기 추가된다.
    return null
  }
```

바로 위에 추가:

```jsx
  if (panel === 'bakeries') return <SavedBakeriesPanel onBack={() => setPanel('home')} />
```

- [ ] **Step 3: CSS 추가**

`src/styles.css` 파일 맨 끝(Task 4 에서 추가한 `/* ===== 마이페이지 ===== */` 블록 다음)에 추가:

```css
.mypage-card-grid{ display:grid; grid-template-columns:repeat(auto-fill,minmax(160px,1fr)); gap:12px; }
.mypage-item-card{ display:flex; flex-direction:column; gap:6px; border:1px solid var(--line); border-radius:14px; padding:14px; background:var(--card); }
.mypage-item-name{ font-weight:700; color:var(--brown); }
.mypage-item-sub{ font-size:.8rem; color:var(--muted); }
```

- [ ] **Step 4: 수동 확인 — 개발 서버**

Run: `npm run dev`

브라우저에서:
1. 로그인 상태에서 "빵 지도" 또는 "빵집 찾기" 결과에서 빵집 하나 이상 "찜하기"
2. "마이페이지" → "찜한 빵 목록" 클릭 → 방금 찜한 빵집이 카드로 보이는지 확인
3. 카드의 "❤️ 찜 해제" 클릭 → 목록에서 사라지는지, "빵 지도"로 돌아가서도 찜 해제가 반영됐는지 확인
4. 찜한 게 하나도 없을 때 "아직 찜한 빵집이 없어요." 문구가 뜨는지 확인

- [ ] **Step 5: Commit**

```bash
git add src/components/mypage/SavedBakeriesPanel.jsx src/pages/MyPage.jsx src/styles.css
git commit -m "[FE] [FEAT] 마이페이지 찜한 빵 목록 패널 추가"
```

---

## Task 6: 찜한 코스 목록 패널

**Files:**
- Create: `src/hooks/useSavedCourses.js`
- Create: `src/components/mypage/SavedCourseMap.jsx`
- Create: `src/components/mypage/SavedCoursesPanel.jsx`
- Modify: `src/pages/MyPage.jsx`
- Modify: `src/styles.css` (끝에 추가)

**Interfaces:**
- Consumes: `useAuth()` (Task 2), `formatCourseLabel` (Task 1), `useKakaoLoader()` (기존, `src/hooks/useKakaoLoader.js`)
- Produces: `useSavedCourses(): { courses: Array<{id, title, travel_mode, stops, origin, created_at}>, loading: boolean, removeCourse: (id: string) => void }`
- Produces: `<SavedCourseMap origin={{lat,lng}} stops={Array<{lat,lng,type,name}>} />`
- Produces: `<SavedCoursesPanel onBack={() => void} />`

- [ ] **Step 1: `useSavedCourses.js` 작성**

```js
// src/hooks/useSavedCourses.js
import { useEffect, useState } from 'react'
import { useAuth } from './useAuth'
import { supabase } from '../lib/supabase'

// 저장된 "대전한바퀴" 코스 목록. 로그인 필수 — 비로그인 시 항상 빈 배열.
// (PilgrimagePage.handleSave 가 insert 하는 saved_courses 테이블을 읽기만 한다.)
export function useSavedCourses() {
  const { user } = useAuth()
  const [courses, setCourses] = useState([])
  const [loading, setLoading] = useState(false)

  useEffect(() => {
    if (!user) {
      setCourses([])
      return
    }
    let alive = true
    setLoading(true)
    supabase
      .from('saved_courses')
      .select('*')
      .eq('user_id', user.id)
      .order('created_at', { ascending: false })
      .then(({ data, error }) => {
        if (!alive) return
        setLoading(false)
        if (error) {
          console.error('[찜한 코스] 조회 실패', error)
          return
        }
        setCourses(data)
      })
    return () => {
      alive = false
    }
  }, [user])

  const removeCourse = (id) => {
    setCourses((prev) => prev.filter((c) => c.id !== id))
    supabase
      .from('saved_courses')
      .delete()
      .eq('id', id)
      .then(({ error }) => error && console.error('[찜한 코스] 삭제 실패', error))
  }

  return { courses, loading, removeCourse }
}
```

- [ ] **Step 2: `SavedCourseMap.jsx` 작성**

`PilgrimagePage.jsx` 내부의 (export 되지 않은) `RouteMap` 컴포넌트를 참고해 만든 읽기 전용 버전이다. `saved_courses` 는 `legPaths`(실제 이동경로)를 저장하지 않으므로 항상 직선 점선으로만 잇는다.

```jsx
// src/components/mypage/SavedCourseMap.jsx
import { useEffect, useRef } from 'react'
import { useKakaoLoader } from '../../hooks/useKakaoLoader'

// 저장된 코스 상세 지도 — origin + stops 를 번호 핀으로 찍고 점선으로 잇는다.
// PilgrimagePage 의 RouteMap 과 달리 legPaths(실제 이동경로)는 저장 시점에 남기지 않으므로
// 항상 직선 점선으로만 그린다.
export default function SavedCourseMap({ origin, stops }) {
  const { loaded, error } = useKakaoLoader()
  const containerRef = useRef(null)
  const mapRef = useRef(null)
  const overlaysRef = useRef([])
  const polylinesRef = useRef([])

  useEffect(() => {
    if (!loaded || !containerRef.current || !origin) return
    const { kakao } = window

    if (!mapRef.current) {
      mapRef.current = new kakao.maps.Map(containerRef.current, {
        center: new kakao.maps.LatLng(origin.lat, origin.lng),
        level: 7,
      })
    }
    const map = mapRef.current

    overlaysRef.current.forEach((o) => o.setMap(null))
    overlaysRef.current = []
    polylinesRef.current.forEach((p) => p.setMap(null))
    polylinesRef.current = []

    const points = [origin, ...stops]
    const bounds = new kakao.maps.LatLngBounds()

    for (let i = 0; i < points.length - 1; i++) {
      const path = [points[i], points[i + 1]].map((p) => new kakao.maps.LatLng(p.lat, p.lng))
      path.forEach((ll) => bounds.extend(ll))
      polylinesRef.current.push(
        new kakao.maps.Polyline({
          map,
          path,
          strokeWeight: 4,
          strokeColor: '#F2814A',
          strokeOpacity: 0.85,
          strokeStyle: 'shortdash',
        }),
      )
    }

    const originOverlay = new kakao.maps.CustomOverlay({
      map,
      position: new kakao.maps.LatLng(origin.lat, origin.lng),
      content: '<div class="pil-pin pil-pin-origin">S</div>',
      yAnchor: 0.5,
    })
    overlaysRef.current.push(originOverlay)
    bounds.extend(new kakao.maps.LatLng(origin.lat, origin.lng))

    stops.forEach((stop, i) => {
      const overlay = new kakao.maps.CustomOverlay({
        map,
        position: new kakao.maps.LatLng(stop.lat, stop.lng),
        content: `<div class="pil-pin pil-pin-${stop.type}">${i + 1}</div>`,
        yAnchor: 0.5,
      })
      overlaysRef.current.push(overlay)
      bounds.extend(new kakao.maps.LatLng(stop.lat, stop.lng))
    })

    if (points.length > 0) map.setBounds(bounds)
  }, [loaded, origin, stops])

  return (
    <div className="map-wrap mypage-course-map">
      {error && (
        <div className="map-error">
          지도를 불러오지 못했습니다.<br />
          <code>VITE_KAKAO_JS_KEY</code> 를 확인하세요.
        </div>
      )}
      <div ref={containerRef} className="map-canvas" />
    </div>
  )
}
```

- [ ] **Step 3: `SavedCoursesPanel.jsx` 작성**

```jsx
// src/components/mypage/SavedCoursesPanel.jsx
import { useState } from 'react'
import { useSavedCourses } from '../../hooks/useSavedCourses'
import { formatCourseLabel } from '../../lib/courseLabel'
import SavedCourseMap from './SavedCourseMap'

export default function SavedCoursesPanel({ onBack }) {
  const { courses, loading, removeCourse } = useSavedCourses()
  const [selectedId, setSelectedId] = useState(null)

  const selected = courses.find((c) => c.id === selectedId)

  if (selected) {
    return (
      <div className="mypage-panel">
        <div className="mypage-panel-header">
          <button type="button" className="mypage-back" onClick={() => setSelectedId(null)}>
            ‹
          </button>
          <h3>{formatCourseLabel(selected)}</h3>
        </div>
        <SavedCourseMap origin={selected.origin} stops={selected.stops} />
        <ol className="mypage-course-stops">
          {selected.stops.map((stop, i) => (
            <li key={stop.id ?? i}>
              {i + 1}. {stop.name}
            </li>
          ))}
        </ol>
        <button
          type="button"
          className="ghost-btn"
          onClick={() => {
            removeCourse(selectedId)
            setSelectedId(null)
          }}
        >
          코스 삭제
        </button>
      </div>
    )
  }

  return (
    <div className="mypage-panel">
      <div className="mypage-panel-header">
        <button type="button" className="mypage-back" onClick={onBack}>
          ‹
        </button>
        <h3>찜한 코스 목록{courses.length > 0 && ` (${courses.length}개)`}</h3>
      </div>
      {loading ? (
        <p className="saved-empty">불러오는 중…</p>
      ) : courses.length === 0 ? (
        <p className="saved-empty">아직 저장한 코스가 없어요. 대전한바퀴에서 코스를 저장해보세요.</p>
      ) : (
        <ul className="saved-list">
          {courses.map((c) => (
            <li key={c.id} className="saved-item">
              <button
                type="button"
                className="mypage-course-item-btn"
                onClick={() => setSelectedId(c.id)}
              >
                {formatCourseLabel(c)}
              </button>
            </li>
          ))}
        </ul>
      )}
    </div>
  )
}
```

- [ ] **Step 4: `MyPage.jsx` 에 연결**

`src/pages/MyPage.jsx` 상단 import 에 추가:

```js
import SavedCoursesPanel from '../components/mypage/SavedCoursesPanel'
```

`if (panel === 'bakeries') return ...` 바로 아래에 추가:

```jsx
  if (panel === 'courses') return <SavedCoursesPanel onBack={() => setPanel('home')} />
```

- [ ] **Step 5: CSS 추가**

`src/styles.css` 파일 맨 끝(Task 5 에서 추가한 블록 다음)에 추가:

```css
.mypage-course-map{ height:360px; margin-bottom:16px; }
.mypage-course-stops{ margin:0 0 16px; padding-left:20px; color:var(--ink); }
.mypage-course-stops li{ margin-bottom:4px; }
.mypage-course-item-btn{ display:block; width:100%; text-align:left; background:none; border:none; font-weight:700; color:var(--brown); cursor:pointer; padding:0; font-size:1rem; }
```

- [ ] **Step 6: 수동 확인 — 개발 서버**

Run: `npm run dev`

브라우저에서:
1. 로그인 상태에서 "빵집 찾기"와 "관광모아" 설문을 모두 완료 → "대전한바퀴" 진입 → "코스 저장하기"
2. "마이페이지" → "찜한 코스 목록" 클릭 → 방금 저장한 코스가 리스트에 라벨(예: "대전한바퀴 · 8/18 저장 · 4곳 · 자동차")로 보이는지 확인
3. 코스 클릭 → 지도에 출발지(S) + 번호 핀 + 점선 경로가 그려지는지, 아래 스탑 목록이 순서대로 보이는지 확인
4. "코스 삭제" 클릭 → 목록에서 사라지는지 확인
5. 저장한 코스가 없을 때 안내 문구가 뜨는지 확인

- [ ] **Step 7: Commit**

```bash
git add src/hooks/useSavedCourses.js src/components/mypage/SavedCourseMap.jsx src/components/mypage/SavedCoursesPanel.jsx src/pages/MyPage.jsx src/styles.css
git commit -m "[FE] [FEAT] 마이페이지 찜한 코스 목록 패널 추가"
```

---

## Task 7: 기록장 패널 (빵집 연결)

**Files:**
- Create: `src/hooks/useDiaryEntries.js`
- Create: `src/components/mypage/DiaryEntryModal.jsx`
- Create: `src/components/mypage/DiaryPanel.jsx`
- Modify: `src/components/map/RecommendCard.jsx`
- Modify: `src/pages/MyPage.jsx`
- Modify: `src/styles.css` (끝에 추가)

**Interfaces:**
- Consumes: `useAuth()` (Task 2), `diary_entries` 테이블 (Task 3 — Supabase 대시보드에서 수동 실행 완료되어 있어야 실제 저장/조회가 동작함)
- Produces: `useDiaryEntries(): { entries: Array<{id, bakery_id, bakery, text, created_at, updated_at}>, loading: boolean, addEntry: (bakery, text) => Promise<void>, updateEntry: (id, text) => Promise<void>, removeEntry: (id) => void }`
- Produces: `<DiaryEntryModal bakery={...} onClose={() => void} onSubmit={(text) => Promise<void>} />`
- Produces: `<DiaryPanel onBack={() => void} />`
- Produces: `RecommendCard` 에 "📝 기록 남기기" 버튼 (로그인 상태에서만 노출)

- [ ] **Step 1: `useDiaryEntries.js` 작성**

```js
// src/hooks/useDiaryEntries.js
import { useEffect, useState } from 'react'
import { useAuth } from './useAuth'
import { supabase } from '../lib/supabase'

// 빵집 연결 기록장. 로그인 필수 — 비로그인 시 항상 빈 배열.
export function useDiaryEntries() {
  const { user } = useAuth()
  const [entries, setEntries] = useState([])
  const [loading, setLoading] = useState(false)

  const reload = () => {
    if (!user) {
      setEntries([])
      return
    }
    setLoading(true)
    supabase
      .from('diary_entries')
      .select('*')
      .eq('user_id', user.id)
      .order('created_at', { ascending: false })
      .then(({ data, error }) => {
        setLoading(false)
        if (error) {
          console.error('[기록장] 조회 실패', error)
          return
        }
        setEntries(data)
      })
  }

  useEffect(reload, [user])

  const addEntry = (bakery, text) => {
    if (!user) return Promise.resolve()
    return supabase
      .from('diary_entries')
      .insert({ user_id: user.id, bakery_id: bakery.id, bakery, text })
      .then(({ error }) => {
        if (error) {
          console.error('[기록장] 작성 실패', error)
          return
        }
        reload()
      })
  }

  const updateEntry = (id, text) => {
    setEntries((prev) => prev.map((e) => (e.id === id ? { ...e, text } : e)))
    return supabase
      .from('diary_entries')
      .update({ text, updated_at: new Date().toISOString() })
      .eq('id', id)
      .then(({ error }) => error && console.error('[기록장] 수정 실패', error))
  }

  const removeEntry = (id) => {
    setEntries((prev) => prev.filter((e) => e.id !== id))
    supabase
      .from('diary_entries')
      .delete()
      .eq('id', id)
      .then(({ error }) => error && console.error('[기록장] 삭제 실패', error))
  }

  return { entries, loading, addEntry, updateEntry, removeEntry }
}
```

- [ ] **Step 2: `DiaryEntryModal.jsx` 작성**

`AuthMenu.jsx` 의 `LoginModal` 과 같은 방식(포탈 + `.auth-modal-overlay`)으로 만든다.

```jsx
// src/components/mypage/DiaryEntryModal.jsx
import { useEffect, useState } from 'react'
import { createPortal } from 'react-dom'

// 빵집 상세 카드(RecommendCard)에서 "기록 남기기" 클릭 시 뜨는 텍스트 작성 모달.
export default function DiaryEntryModal({ bakery, onClose, onSubmit }) {
  const [text, setText] = useState('')
  const [saving, setSaving] = useState(false)

  useEffect(() => {
    const onKey = (e) => e.key === 'Escape' && onClose()
    document.addEventListener('keydown', onKey)
    document.body.style.overflow = 'hidden'
    return () => {
      document.removeEventListener('keydown', onKey)
      document.body.style.overflow = ''
    }
  }, [onClose])

  const submit = async (e) => {
    e.preventDefault()
    const trimmed = text.trim()
    if (!trimmed) return
    setSaving(true)
    await onSubmit(trimmed)
    setSaving(false)
    onClose()
  }

  return createPortal(
    <div className="auth-modal-overlay" onClick={onClose}>
      <div className="auth-modal diary-modal" onClick={(e) => e.stopPropagation()}>
        <button type="button" className="auth-modal-close" aria-label="닫기" onClick={onClose}>
          ✕
        </button>
        <div className="auth-modal-copy">
          <span className="auth-modal-emoji" aria-hidden="true">📝</span>
          <h3 className="auth-modal-title">{bakery.name} 기록 남기기</h3>
        </div>
        <form className="diary-modal-form" onSubmit={submit}>
          <textarea
            className="diary-modal-textarea"
            value={text}
            onChange={(e) => setText(e.target.value)}
            placeholder="오늘 이 빵집에서 어떤 빵을 먹었나요?"
            rows={5}
            autoFocus
          />
          <button type="submit" className="primary-btn" disabled={saving || !text.trim()}>
            {saving ? '저장 중…' : '기록하기'}
          </button>
        </form>
      </div>
    </div>,
    document.body,
  )
}
```

- [ ] **Step 3: `RecommendCard.jsx` 에 "기록 남기기" 버튼 추가**

`src/components/map/RecommendCard.jsx` 상단 import(1-5번째 줄)에 추가:

```js
import { useAuth } from '../../hooks/useAuth'
import { useDiaryEntries } from '../../hooks/useDiaryEntries'
import DiaryEntryModal from '../mypage/DiaryEntryModal'
```

컴포넌트 내부, `const { toggleSave, isSaved } = useSavedBakeries()` (현재 11번째 줄) 다음에 추가:

```js
  const { user } = useAuth()
  const { addEntry } = useDiaryEntries()
  const [diaryOpen, setDiaryOpen] = useState(false)
```

`rec-name-row` 블록(현재 36-45번째 줄) 바로 다음에 새 줄 추가:

```jsx
      {user && (
        <div className="rec-actions">
          <button type="button" className="save-btn" onClick={() => setDiaryOpen(true)}>
            📝 기록 남기기
          </button>
        </div>
      )}
```

컴포넌트 최종 `return (...)` 의 맨 바깥 `</div>` 직전(현재 66번째 줄, `카카오맵에서 보기` 링크 블록 다음)에 추가:

```jsx
      {diaryOpen && (
        <DiaryEntryModal
          bakery={bakery}
          onClose={() => setDiaryOpen(false)}
          onSubmit={(text) => addEntry(bakery, text)}
        />
      )}
```

- [ ] **Step 4: `DiaryPanel.jsx` 작성**

```jsx
// src/components/mypage/DiaryPanel.jsx
import { useState } from 'react'
import { useDiaryEntries } from '../../hooks/useDiaryEntries'

function formatDate(iso) {
  const d = new Date(iso)
  return `${d.getFullYear()}. ${String(d.getMonth() + 1).padStart(2, '0')}. ${String(d.getDate()).padStart(2, '0')}`
}

// 마이페이지 기록장 패널. 작성은 RecommendCard(빵집 상세)의 "기록 남기기"에서만 가능
// — 여기선 목록 보기 / 수정 / 삭제만 한다.
export default function DiaryPanel({ onBack }) {
  const { entries, loading, updateEntry, removeEntry } = useDiaryEntries()
  const [selectedId, setSelectedId] = useState(null)
  const [editing, setEditing] = useState(false)
  const [draft, setDraft] = useState('')

  const selected = entries.find((e) => e.id === selectedId)

  const openEntry = (entry) => {
    setSelectedId(entry.id)
    setEditing(false)
    setDraft(entry.text)
  }

  const submitEdit = (e) => {
    e.preventDefault()
    const trimmed = draft.trim()
    if (!trimmed) return
    updateEntry(selectedId, trimmed)
    setEditing(false)
  }

  if (selected) {
    return (
      <div className="mypage-panel">
        <div className="mypage-panel-header">
          <button type="button" className="mypage-back" onClick={() => setSelectedId(null)}>
            ‹
          </button>
          <h3>기록장</h3>
        </div>
        <div className="diary-detail">
          <div className="diary-detail-meta">
            <span className="diary-detail-bakery">{selected.bakery?.name}</span>
            <span>{formatDate(selected.created_at)}</span>
          </div>
          {editing ? (
            <form onSubmit={submitEdit} className="diary-modal-form">
              <textarea
                className="diary-modal-textarea"
                value={draft}
                onChange={(e) => setDraft(e.target.value)}
                rows={6}
                autoFocus
              />
              <div className="mypage-nickname-actions">
                <button type="submit" className="primary-btn">
                  저장
                </button>
                <button type="button" className="ghost-btn" onClick={() => setEditing(false)}>
                  취소
                </button>
              </div>
            </form>
          ) : (
            <>
              <p className="diary-detail-text">{selected.text}</p>
              <div className="mypage-nickname-actions">
                <button type="button" className="ghost-btn" onClick={() => setEditing(true)}>
                  수정하기
                </button>
                <button
                  type="button"
                  className="ghost-btn"
                  onClick={() => {
                    removeEntry(selectedId)
                    setSelectedId(null)
                  }}
                >
                  삭제
                </button>
              </div>
            </>
          )}
        </div>
      </div>
    )
  }

  return (
    <div className="mypage-panel">
      <div className="mypage-panel-header">
        <button type="button" className="mypage-back" onClick={onBack}>
          ‹
        </button>
        <h3>기록장</h3>
      </div>
      {loading ? (
        <p className="saved-empty">불러오는 중…</p>
      ) : entries.length === 0 ? (
        <p className="saved-empty">아직 기록이 없어요. 빵집 상세에서 "기록 남기기"로 남겨보세요.</p>
      ) : (
        <div className="diary-grid">
          {entries.map((entry) => (
            <button
              type="button"
              key={entry.id}
              className="diary-card"
              onClick={() => openEntry(entry)}
            >
              <span className="diary-card-date">{formatDate(entry.created_at)}</span>
              <span className="diary-card-bakery">{entry.bakery?.name}</span>
              <p className="diary-card-text">{entry.text}</p>
            </button>
          ))}
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 5: `MyPage.jsx` 에 연결**

`src/pages/MyPage.jsx` 상단 import 에 추가:

```js
import DiaryPanel from '../components/mypage/DiaryPanel'
```

`if (panel === 'courses') return ...` 바로 아래에 추가:

```jsx
  if (panel === 'diary') return <DiaryPanel onBack={() => setPanel('home')} />
```

- [ ] **Step 6: CSS 추가**

`src/styles.css` 파일 맨 끝(Task 6 에서 추가한 블록 다음)에 추가:

```css
.rec-actions{ margin-top:6px; }
.diary-modal-form{ display:flex; flex-direction:column; gap:12px; width:100%; }
.diary-modal-textarea{ width:100%; padding:12px; border:1.5px solid var(--line); border-radius:12px; font-family:inherit; font-size:.9rem; resize:vertical; }
.diary-grid{ display:grid; grid-template-columns:repeat(auto-fill,minmax(200px,1fr)); gap:12px; }
.diary-card{ display:flex; flex-direction:column; gap:6px; align-items:flex-start; border:1px solid var(--line); border-radius:14px; padding:14px; background:var(--card); text-align:left; cursor:pointer; }
.diary-card:hover{ border-color:var(--accent); }
.diary-card-date{ font-size:.75rem; color:var(--muted); }
.diary-card-bakery{ font-weight:700; color:var(--brown); }
.diary-card-text{ margin:0; font-size:.85rem; color:var(--ink); overflow:hidden; text-overflow:ellipsis; display:-webkit-box; -webkit-line-clamp:3; -webkit-box-orient:vertical; }
.diary-detail-meta{ display:flex; justify-content:space-between; margin-bottom:12px; color:var(--muted); font-size:.85rem; }
.diary-detail-bakery{ font-weight:700; color:var(--brown); }
.diary-detail-text{ white-space:pre-wrap; line-height:1.6; color:var(--ink); }
```

- [ ] **Step 7: 수동 확인 — 개발 서버 (Task 3 의 `diary_entries` 테이블이 Supabase 에 이미 생성되어 있어야 함)**

Run: `npm run dev`

브라우저에서:
1. 로그인 상태에서 "빵 지도"에서 빵집 하나 선택 → 상세 카드에 "📝 기록 남기기" 버튼이 보이는지 확인 (비로그인 시 안 보이는지도 확인)
2. 클릭 → 모달에서 텍스트 입력 → "기록하기" → 모달 닫힘 확인
3. "마이페이지" → "기록장" → 방금 쓴 기록이 카드로(날짜/빵집명/미리보기 텍스트) 보이는지 확인
4. 카드 클릭 → 상세에서 전체 텍스트 확인 → "수정하기" → 텍스트 변경 → "저장" → 반영 확인
5. "삭제" → 목록에서 사라지는지 확인
6. 기록이 없을 때 안내 문구 확인

- [ ] **Step 8: Commit**

```bash
git add src/hooks/useDiaryEntries.js src/components/mypage/DiaryEntryModal.jsx src/components/mypage/DiaryPanel.jsx src/components/map/RecommendCard.jsx src/pages/MyPage.jsx src/styles.css
git commit -m "[FE] [FEAT] 마이페이지 기록장 패널 및 빵집 상세 기록 남기기 추가"
```

---

## Task 8: 정리 — 구 `SavedListPage` 제거 + 전체 회귀 확인

**Files:**
- Delete: `src/pages/SavedListPage.jsx`

**Interfaces:**
- Consumes: 모든 이전 Task의 결과물
- Produces: 없음 (정리 + 검증 전용 태스크)

- [ ] **Step 1: 남은 참조 확인**

Run: `grep -rn "SavedListPage" src`
Expected: 결과 없음 (Task 4 Step 4 에서 `LandingPage.jsx`의 import/사용을 이미 `MyPage`로 교체했으므로, `SavedListPage.jsx` 자기 자신의 정의 외엔 참조가 없어야 한다)

- [ ] **Step 2: 파일 삭제**

```bash
git rm src/pages/SavedListPage.jsx
```

- [ ] **Step 3: 전체 테스트 스위트 실행**

Run: `npm test`
Expected: PASS (Task 1 의 신규 테스트 포함 전체 통과)

- [ ] **Step 4: 빌드 확인**

Run: `npm run build`
Expected: 에러 없이 빌드 성공 (미사용 import, 깨진 참조 등 없는지 확인)

- [ ] **Step 5: 수동 회귀 확인 — 개발 서버 (spec 의 테스트 방법 전체)**

Run: `npm run dev`

1. 로그인 상태에서 찜한 빵 추가 → 마이페이지에서 확인
2. 대전한바퀴 코스 저장 → 마이페이지 "찜한 코스 목록"에 표시되는지, 상세에서 지도/순서가 맞는지
3. 빵집 상세 카드에서 "기록 남기기" → 텍스트 작성 → 마이페이지 기록장에 표시 → 상세에서 "수정하기"로 텍스트 변경 확인
4. 닉네임 변경 → AuthMenu(상단)와 마이페이지 프로필 카드 양쪽에 반영되는지
5. 친구목록 탭 클릭 시 placeholder 노출 확인
6. 비로그인 상태에서 마이페이지 진입 시 로그인 유도 문구 확인
7. 모바일 너비에서 4개 패널 레이아웃 확인
8. 기존 기능 회귀 확인: "빵집 찾기" 설문 → 결과, "빵 지도", "관광모아", "대전한바퀴" 화면이 각각 정상 진입되는지 (NavBar/LandingPage 수정으로 인한 사이드이펙트 없는지)

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "[FE] [FIX] 구 SavedListPage 제거 및 마이페이지 전체 회귀 확인"
```

---

## 완료 후

이슈 #20 을 닫을 PR을 `develop` 브랜치로 생성한다 (`workflow_github_collab` 컨벤션: `Closes #20`, 변경 요약, 테스트 방법 포함). 사용자가 PR 링크를 팀 리뷰용으로 공유한 뒤 승인되면 본인이 머지한다.
