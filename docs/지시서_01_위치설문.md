# 지시서 #1 — 위치 설문 · 위치 정보 수집 (be-setup2)

작성 시각: 2026-07-25 (v2 재작성)
브랜치: `be-setup2` (develop 100% 베이스) · 파일 루트: **C:\Users\user\projects\bbangmoa (고정)**
배포: push → PR → develop → breadmoa (내 CLI 프로덕션 배포 안 함. 프리뷰만 필요시)

## 1. 결론

develop의 설문 0단계는 **"구(district) 선택"** 하나뿐이라 **좌표가 없다**. 이걸 **우리 위치 질문(정밀 출발지: 프리셋 + GPS + 지도 직접 찍기)으로 그대로 교체**한다. 구를 프리셋에 매핑하는 게 아니라, **우리 개선판 질문을 통째로 넣는다.**

핵심 이유: 여기서 얻는 **정밀 좌표 `origin`이 설문 이후 결과에도 흐른다.** 즉 "오늘의 빵 → 지도 → (나중) 여행 도착지"가 **실제 내 위치 기준**으로 현실화된다. 구 단위(coarse)로는 이게 안 된다.

**이번 지시서는 origin 확보 + 설문 흐름 통합까지.** 경로/하루 코스·영업시간·로직 최종본은 범위 밖(§8).

## 2. 현재 develop 구조 (건드릴 지점)

| 파일 | 현재 동작 | 이번 변경 |
|---|---|---|
| `pages/LandingPage.jsx` | `stage: survey→reveal→map`. 완료판정 `surveyDone = !!district && SURVEY.every(...)` | `district` → `origin` 기준으로 |
| `components/survey/SurveyFlow.jsx` | step 0 = 구 버튼(`region.districts`) | step 0을 **LocationStep**으로 교체 |
| `store/useAppStore.js` | `district·answers·regionId·selectedBakeryId` (**origin 없음**) | `origin` + `setOrigin` 추가 |
| `config/regions.js` | `districts`만, **origins 없음** | `origins` 프리셋 추가 |
| `data/stations.js` | 대전역·서대전역(외부 거리기준) | 프리셋 좌표 정합(값 일치) |
| `hooks/useCurrentLocation.js` | GPS+역지오코딩(현재 MapResult에서만) | LocationStep에서 재사용 |
| `components/map/MapResult.jsx` | 거리 기준점을 GPS/역으로 잡음 | **origin 우선 사용**(설문서 고른 위치) |

## 3. 데이터 계약 (origin)

```text
origin = {
  lat, lng,             // 정밀 좌표
  label,                // "대전역" / "현재 위치" / "지도에서 선택"
  source: 'preset' | 'gps' | 'pick'
}
```
- store: `origin: null` 기본 + `setOrigin(origin)` + `resetAnswers`에 `origin:null`.
- **origin은 결과로 흐른다**: `MapResult`가 거리 계산·정렬·(추후)도착지 기준을 GPS/역 대신 **origin 우선**으로.

## 4. 상세 동작 명세 — LocationStep (설문 0단계 = 우리 질문 그대로)

제목: **"어디서 출발하세요?"** 세 방법 모두 제공:

### 4.1 프리셋 버튼 (regions.daejeon.origins)
```text
🚉 대전역(36.33184,127.43417) · 🚉 서대전역(36.31984,127.40287)
🚌 복합터미널(36.3505,127.4348) · 🚌 유성터미널(36.354,127.341)
🏛️ 시청(36.3504,127.3845) · 🏙️ 둔산(36.3515,127.3776)
```
클릭 → `setOrigin({...preset, source:'preset'})` → 다음 단계.

### 4.2 📍 현재 위치 사용 (GPS)
`useCurrentLocation` → 좌표 오면 `setOrigin({lat,lng,label:'현재 위치',source:'gps'})`. 거부/미지원 → "권한 필요" 안내 + 프리셋/지도 유도(앱 안 멈춤).

### 4.3 🗺️ 지도에서 직접 찍기 (이번 범위 포함)
- **대전 밖이든 안이든** 사용 가능. 작은 카카오맵 → 클릭 지점 `setOrigin({lat,lng,label:'지도에서 선택',source:'pick'})`.
- 확정 버튼으로 다음 단계.

### 4.4 SurveyFlow 통합
- step 0 = `<LocationStep onDone={()=>setStep(1)} />` (구 버튼 삭제).
- step≥1 = 기존 SURVEY 문항 그대로.
- 진행바 카운트 동일(0번 의미만 교체).

## 5. 파일별 변경 명세
1. `store/useAppStore.js` — `origin`/`setOrigin`, reset에 초기화.
2. `config/regions.js` — `daejeon.origins`(§4.1).
3. `components/survey/LocationStep.jsx` (신규) — 프리셋+GPS+지도찍기. develop 클래스(survey-step/option) 톤 맞춤.
4. `components/survey/SurveyFlow.jsx` — step 0 교체.
5. `pages/LandingPage.jsx` — `surveyDone` 판정 origin 기준.
6. `components/map/MapResult.jsx` — 거리 기준점 origin 우선(GPS/역은 폴백).
7. `styles.css` — `.loc-*` 최소 스타일.

## 6. ❓ 결정 필요 (나 혼자 안 정함)

**develop 빵집 검색은 `district`(구)로 돈다**(카카오 45개 상한 우회용 구별 루프). 우리 질문은 구가 아니라 좌표를 준다. 그래서 구 선택 UI가 사라지면 검색을 어떻게?
- **(A) 권장**: 구 선택 UI만 제거, 검색은 **전 구(5개) 자동 루프**(develop 코드에 이미 있음) → 결과를 **origin 거리순 정렬**. 사용자는 구를 안 고르고, 내부적으로 다 긁고 가까운 순.
- (B) origin 좌표에서 구를 추정해 그 구만 검색(경계 데이터 필요, 복잡).
→ **A로 가면 될지 확인 요청.** (내 생각도 A)

## 7. 검증 기준 (완료 정의)
- [ ] 설문 0단계 = 프리셋6 + GPS + 지도찍기 표시.
- [ ] 각 방법으로 origin 세팅 → 다음 문항 진행.
- [ ] GPS 거부 시 안내·폴백, 앱 안 멈춤.
- [ ] 결과(MapResult) 거리 기준이 **고른 origin** 반영.
- [ ] reveal/map까지 흐름 안 깨짐 · build 통과 · 콘솔 0.
- [ ] **로컬 `npm run dev` 눈확인**(또는 프리뷰 URL) → 그다음 커밋·push.

## 8. 범위 밖 (이번에 안 함)
경로/하루 코스(보류·문서만) · 영업시간 파싱(따로) · 로직 최종본(안 건드림, develop 태그가중 유지) · 마커/프랜차이즈/가까운관광지(#2·#3·#4).

## 9. 다음 작업 (순서)
1. store origin → 2. regions.origins → 3. LocationStep(3방법) → 4. SurveyFlow step0 교체 → 5. LandingPage surveyDone → 6. MapResult origin 우선 → 7. build+로컬 검증 → 커밋 `feat: 출발 위치 설문 단계(프리셋·GPS·지도찍기) + origin 반영` → push be-setup2 → 복귀.
